node_modules/
dist/
*.log
.DS_Store
Thumbs.db
reference/
testing/
local-experiments/
CLAUDE.md
graphify-out

# Firefox/web-ext build output and packaged artifacts — generated by
# `npm run build:firefox`, never hand-edited, shouldn't be committed.
web-ext-artifacts/
*.xpi
*.zip

# Local tool config / secrets — never commit credentials.
.claude/
.env
.env.*
!.env.example
# Local-only documentation and research may contain generated notes, saved page
# captures, school-specific material, or standalone experiments. Never commit
# or package anything under this directory.
docs/
PROJECT_CONTEXT.md
AGENTS.md
